// P0-Client ships no feature UI yet. A real widget test is deferred to P2;
// this placeholder keeps `flutter test` passing and prevents the default
// boilerplate from referencing the removed MyApp widget.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — widget tests added in P2', () {
    expect(1 + 1, 2);
  });
}
